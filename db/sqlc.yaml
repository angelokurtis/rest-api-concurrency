version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "postgres"
        out: "../internal/postgres"
        emit_interface: true
        emit_json_tags: true
        emit_empty_slices: true
        emit_prepared_queries: false
