version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "postgres"
        out: "../internal/postgres"
        emit_interface: true
        emit_json_tags: false
        emit_empty_slices: false
        emit_prepared_queries: false
